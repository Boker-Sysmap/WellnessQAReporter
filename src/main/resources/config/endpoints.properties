###############################################################################
# ? WellnessQA - Configura��o de Endpoints Ativos (endpoints.properties)
#
# Este arquivo define quais endpoints da API Qase ser�o consultados durante
# a execu��o do WellnessQA Reporter.
#
# Cada linha representa um endpoint e o valor booleano indica se est� ativo:
#   - true  ? o endpoint ser� consultado na coleta de dados.
#   - false ? o endpoint ser� ignorado.
#
# ?? Funcionamento:
#   O ConfigManager l� este arquivo durante a inicializa��o do sistema.
#   Ele substitui a lista de endpoints definida em config.properties
#   (chave: qase.endpoints), caso este arquivo exista.
#
# ? Boas Pr�ticas:
#   - Mantenha apenas endpoints realmente necess�rios ativos, para reduzir
#     tempo de coleta e tamanho do relat�rio.
#   - Use true ou false para desativar temporariamente endpoints.
#   - Recomenda-se revisar esta lista conforme a evolu��o da API Qase
###############################################################################


###############################################################################
# ? Endpoints principais de teste
###############################################################################

# Casos de teste (estrutura base de todos os relat�rios)
case=true

# Su�tes (agrupamentos funcionais de casos)
suite=true

# Resultados de execu��o (execu��es associadas a runs)
result=true

# Defeitos (bugs registrados no Qase)
defect=true

# Marcos (milestones) associados aos testes
milestone=true

# Execu��es (runs) de testes
run=true

# Planos de teste (conjunto de casos planejados)
plan=false

# Configura��es (custom fields, par�metros, etc.)
configuration=true

# Ambientes de execu��o (dev, staging, prod, etc.)
environment=true

# Usu�rios (mapeamento de autores e respons�veis)
user=true


###############################################################################
# ? Fim do arquivo endpoints.properties
###############################################################################
